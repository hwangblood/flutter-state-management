type UsersSchema = {
  /**
   * user's unique id
   */
  uid: string;
  /**
   * user's unique email address
   */
  email: string;
  /**
   * user's display name
   */
  display_name: string;
};

export default UsersSchema;
